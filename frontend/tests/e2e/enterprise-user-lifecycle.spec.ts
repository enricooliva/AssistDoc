import { test } from '@playwright/test';

test.describe('Enterprise user lifecycle', () => {
  test('placeholder for unified login, user search, and soft delete admin flows', async () => {
    test.skip();
  });
});
