import { test } from '@playwright/test';

test.describe('Enterprise user lifecycle', () => {
  test('placeholder for unified login and enterprise user lifecycle flows', async () => {
    test.skip();
  });
});
